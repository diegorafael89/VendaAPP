rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // As rotas de API do backend Express realizam autenticação RBAC via token/sessão
    // Regras padrão seguras para acesso via Firestore
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
